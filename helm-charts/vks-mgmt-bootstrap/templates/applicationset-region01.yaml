{{- if and .Values.argocd.enabled .Values.argocd.vksclusters.region01.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: {{ .Values.argocd.vksclusters.region01.applicationsetname }}
  namespace: {{ .Values.argocd.applicationset.namespace }}
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - matrix:
      generators:
        - git:
            repoURL: {{ .Values.argocd.applicationset.git.repoURL }}
            revision: {{ .Values.argocd.applicationset.git.targetRevision }}
            directories:
            - path: {{ .Values.argocd.applicationset.git.path }}
        - clusters:    
{{- with .Values.argocd.vksclusters.region01.selector }} 
            selector:
{{ toYaml . | indent 14 -}}
{{ end }}             
  template:      
    metadata:
      name: {{ printf "{{ (split \":\" .name)._0 }}-{{index .path.segments 3}}" | quote }}
    spec:
      project: {{ .Values.argocd.applicationset.project }}
      source:
        repoURL: {{ .Values.argocd.applicationset.git.repoURL }}
        targetRevision: {{ .Values.argocd.applicationset.git.targetRevision }}
        path: {{ printf "{{.path.path}}" | quote }}
        helm:
          valueFiles:
            - {{ .Values.argocd.vksclusters.region01.helmValuesFile }}
      destination:
        server: {{ printf "{{.server}}" | quote }}
        namespace: {{ printf "{{index .path.segments 3}}" | quote }}
{{- with .Values.argocd.applicationset.syncPolicy }}
      syncPolicy:
{{ toYaml . | indent 8 -}}
{{ end }}
{{ end }}